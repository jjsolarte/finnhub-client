import 'package:flutter/material.dart';

class Palette {
  static const primaryColor = Colors.deepPurple;
  static const primaryText = Color(0xFFFFFFFF);
  static const primarySubtitle = Color(0xFFBDBDBD);
}